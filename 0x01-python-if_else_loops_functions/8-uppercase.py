#!/usr/bin/python3
def uppercase(str):
    """prints a string in uppercase followed by a new line"""
    for i in range(len(str))
    if ord(c) in range(97, 123):
        return (True)
    else:
        return (False)
