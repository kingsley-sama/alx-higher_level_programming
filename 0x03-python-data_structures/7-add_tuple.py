#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    """sums corresponding elements in tuple_a and tuple_b"""
    my_tuple = []
    while i in range(len(tuple_a)):
        my_tuple[i] = tuple_a[i] + tuple_b[i]
        
